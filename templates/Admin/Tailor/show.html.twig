{% extends 'layout.html.twig' %}

{% block title %}Tailor{% endblock %}

{% block content %}
    <h1>Tailor</h1>

    <table class="table table-striped table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tailor.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_tailor_index') }}">back to list</a>

    <a href="{{ path('admin_tailor_edit', {'id': tailor.id}) }}">edit</a>

    {{ include('Admin/Tailor/_delete_form.html.twig') }}
{% endblock %}
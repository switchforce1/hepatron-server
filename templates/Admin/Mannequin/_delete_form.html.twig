<form method="post" action="{{ path('admin_mannequin_delete', {'id': mannequin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mannequin.id) }}">
    <button class="btn">Delete</button>
</form>
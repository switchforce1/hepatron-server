{% extends 'layout.html.twig' %}

{% block title %}Mannequin{% endblock %}

{% block content %}
    <h1>Mannequin</h1>

    <table class="table table-striped table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mannequin.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_mannequin_index') }}">back to list</a>

    <a href="{{ path('admin_mannequin_edit', {'id': mannequin.id}) }}">edit</a>

    {{ include('Admin/Mannequin/_delete_form.html.twig') }}
{% endblock %}
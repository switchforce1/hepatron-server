{% extends 'layout.html.twig' %}

{% block title %}Seller{% endblock %}

{% block body %}
    <h1>Seller</h1>

    <table class="table table-striped table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ seller.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_seller_index') }}">back to list</a>

    <a href="{{ path('admin_seller_edit', {'id': seller.id}) }}">edit</a>

    {{ include('Admin/Seller/_delete_form.html.twig') }}
{% endblock %}
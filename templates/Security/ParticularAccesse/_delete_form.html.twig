<form method="post" action="{{ path('security_particular_accesse_delete', {'id': particular_accesse.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ particular_accesse.id) }}">
    <button class="btn">Delete</button>
</form>
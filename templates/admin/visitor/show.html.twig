{% extends 'base.html.twig' %}

{% block title %}Visitor{% endblock %}

{% block body %}
    <h1>Visitor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ visitor.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_visitor_index') }}">back to list</a>

    <a href="{{ path('admin_visitor_edit', {'id': visitor.id}) }}">edit</a>

    {{ include('admin_visitor/_delete_form.html.twig') }}
{% endblock %}
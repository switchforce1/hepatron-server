{% extends 'layout.html.twig' %}

{% block title %}Shooting{% endblock %}

{% block body %}
    <h1>Shooting</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shooting.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ shooting.label }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ shooting.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('middle_shooting_index') }}">back to list</a>

    <a href="{{ path('middle_shooting_edit', {'id': shooting.id}) }}">edit</a>

    {{ include('Middle/Shooting/_delete_form.html.twig') }}
{% endblock %}
{% extends 'layout.html.twig' %}

{% block title %}Notice{% endblock %}

{% block body %}
    <h1>Notice</h1>

    <table class="table table-striped table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ notice.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('middle_notice_index') }}">back to list</a>

    <a href="{{ path('middle_notice_edit', {'id': notice.id}) }}">edit</a>

    {{ include('Middle/Notice/_delete_form.html.twig') }}
{% endblock %}
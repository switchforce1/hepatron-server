{% extends 'layout.html.twig' %}

{% block title %}Visibility{% endblock %}

{% block body %}
    <h1>Visibility</h1>

    <table class="table table-striped table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ visibility.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ visibility.label }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ visibility.code }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ visibility.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('middle_visibility_index') }}">back to list</a>

    <a href="{{ path('middle_visibility_edit', {'id': visibility.id}) }}">edit</a>

    {{ include('Middle/Visibility/_delete_form.html.twig') }}
{% endblock %}
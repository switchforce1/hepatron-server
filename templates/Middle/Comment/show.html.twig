{% extends 'layout.html.twig' %}

{% block title %}Comment{% endblock %}

{% block body %}
    <h1>Comment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ comment.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('middle_comment_index') }}">back to list</a>

    <a href="{{ path('middle_comment_edit', {'id': comment.id}) }}">edit</a>

    {{ include('Middle/Comment/_delete_form.html.twig') }}
{% endblock %}
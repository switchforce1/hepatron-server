{% extends 'layout.html.twig' %}

{% block title %}DesignItem{% endblock %}

{% block body %}
    <h1>DesignItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ design_item.id }}</td>
            </tr>
            <tr>
                <th>Required</th>
                <td>{{ design_item.required ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('middle_design_item_index') }}">back to list</a>

    <a href="{{ path('middle_design_item_edit', {'id': design_item.id}) }}">edit</a>

    {{ include('Middle/DesignItem/_delete_form.html.twig') }}
{% endblock %}